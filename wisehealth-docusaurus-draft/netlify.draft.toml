# DRAFT: Example Netlify configuration for a Docusaurus site.
# Copy/adjust into netlify.toml in your real project if needed.

[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "18"

# Example redirect to handle SPA-like routing if necessary.
# [[redirects]]
#   from = "/*"
#   to = "/index.html"
#   status = 200
